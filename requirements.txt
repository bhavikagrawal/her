# Python stack for HER. Requires Python >=3.10 (Kokoro + NumPy 2). Use `brew install python@3.12` if needed.
# Voice + streaming stack (Phase 1): websockets, mic/IO, ONNX TTS/VAD, HTTP to Ollama.
websockets>=13,<14
numpy>=2.0.2
sounddevice>=0.5.0
soundfile>=0.12.0
httpx>=0.27.0
onnxruntime>=1.19.2
kokoro-onnx>=0.4.9
silero-vad-lite>=0.2.1
langdetect>=1.0.9
