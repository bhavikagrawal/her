# Will track which human-life topics still need curious questions (never repeating once asked).
# Picture sticky notes on a cork board: each gap is a reminder to stay genuinely interested.
# Phase 0 stores no SQLite rows yet; onboarding memory lands when profiles do.
# Keeping gaps explicit prevents HER from sounding like a generic survey robot later.

"""Profile gap tracking placeholder for HER."""

from __future__ import annotations

__all__: list[str] = []
