# Package for behaviors that make HER feel present (openers, lulls, background check-ins).
# Proactive code is about time, not just text: it schedules and cancels work on a clock.
# Phase 0 only instantiates the folder; the live app is still the WebSocket proof in `main.py`.
# Grouping these files keeps "when to speak" separate from "how to speak" in `voice/`.

"""Proactive behavior subpackage for HER."""

from __future__ import annotations

__all__: list[str] = []
