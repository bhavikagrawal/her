# Reserved for turning intents into ordered steps (like a calm stage manager backstage).
# Local tools and generated agents plug in here once execution safety checks exist.
# Phase 0 keeps this file unused so imports never cycle during early experiments.
# Separating planning from execution keeps HER easier to test in small pieces.

"""Action planning placeholder module for HER."""

from __future__ import annotations

__all__: list[str] = []
