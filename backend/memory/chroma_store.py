# Will store and search meaning-rich memories for RAG-style context in system prompts.
# Chroma is the scrapbook of stories, not the ID card: semantically close, not always exact.
# Phase 0 never creates a collection; the database files will appear with later features.
# This file will hold only Chroma concerns, not app settings, to keep responsibilities clear.

"""ChromaDB vector memory placeholder for HER."""

from __future__ import annotations

__all__: list[str] = []
