# Will quietly collect clean audio for later voice cloning when enough data exists.
# Think of it as keeping a photo album: only the right moments, over time, with consent.
# Phase 0 does not store audio; this is a contract for how future training data will flow.
# A separate absorber keeps privacy rules and sample quality checks in one place.

"""Voice sample collection placeholder for HER."""

from __future__ import annotations

__all__: list[str] = []
