# Groups microphone capture, transcription, synthesis, VAD, and voice-sample absorption.
# Voice is HER's body language: timing, interruption, and breath matter as much as text.
# Phase 0 has no audio path; this package simply reserves clear names for later imports.
# Splitting files keeps each concern testable without booting the whole desktop shell.

"""Voice pipeline subpackage for HER."""

from __future__ import annotations

__all__: list[str] = []
