# Reserved for writing, validating, and sandbox-running self-built helper scripts.
# Think of it as HER's workshop: blueprints become small tools stored on disk safely.
# Phase 0 leaves the factory idle; the desktop app only proves the network path today.
# This file will stay isolated so untrusted generated code never touches sensitive paths blindly.

"""Agent generation and execution placeholder for HER."""

from __future__ import annotations

__all__: list[str] = []
