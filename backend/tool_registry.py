# Reserved for listing which built-in capabilities HER may call (memory, clocks, etc.).
# Registries are like a kitchen mise en place: everything reachable before cooking starts.
# Phase 0 does not register tools yet because no voice pipeline asks for them.
# Centralizing names prevents duplicate tool IDs across modules when they arrive.

"""Tool discovery placeholder module for HER."""

from __future__ import annotations

__all__: list[str] = []
