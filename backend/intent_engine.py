# Reserved for HER's intent classifier (what you meant across emotional + practical axes).
# It becomes the brainstem that chooses tools and tone once speech and memory exist.
# Phase 0 does not call this file; wiring arrives when the microphone loop lands.
# Name stays intent_engine so later imports match the planned architecture diagram.

"""Intent inference placeholder module for HER."""

from __future__ import annotations

__all__: list[str] = []
