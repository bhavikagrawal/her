# HER local-only artifacts — keep builds, caches, and venv out of git.
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.cursor/
target/
*.log
.DS_Store

# Local runtime data (models, temp audio, caches). Keep `data/` fully out of git.
data/
